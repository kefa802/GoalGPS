<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="12dp"
    android:paddingHorizontal="8dp"
    android:background="#FFFFFF"
    android:gravity="center_vertical">

    <TextView
        android:id="@+id/tvLogName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:textSize="14sp"
        android:textColor="#333333" />

    <TextView
        android:id="@+id/tvLogInTime"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/tvLogOutTime"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:textSize="14sp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1.5"
        android:orientation="horizontal"
        android:gravity="right">
        <Button android:id="@+id/btnRowUp" android:layout_width="30dp" android:layout_height="35dp" android:text="↑" android:textSize="10sp" android:padding="0dp" />
        <Button android:id="@+id/btnRowDown" android:layout_width="30dp" android:layout_height="35dp" android:text="↓" android:textSize="10sp" android:padding="0dp" />
        <Button android:id="@+id/btnRowDel" android:layout_width="30dp" android:layout_height="35dp" android:text="削" android:textSize="10sp" android:padding="0dp" android:backgroundTint="#FF5252" />
    </LinearLayout>
</LinearLayout>
